[target.x86_64-pc-windows-msvc]
linker = "link.exe"
# Note: Windows SDK paths should be set by vcvarsall.bat
# If build fails, ensure Windows SDK is installed via Visual Studio Installer

[target.wasm32-unknown-unknown]
rustflags = ["--cfg=web_sys_unstable_apis"]
